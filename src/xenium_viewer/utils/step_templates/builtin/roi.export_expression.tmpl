# xenium-viewer template
# id: roi.export_expression
# schema-version: 1
# doc: Write the per-cell ROI expression table to CSV.
# requires: roi_expr_cells
# params: gene:str, path:str

#--- block main
# Export ROI per-cell expression of $gene
roi_expr_cells.to_csv($path, index=False)
