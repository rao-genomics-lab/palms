# palms template
# id: spatial_neighbors
# schema-version: 1
# doc: Spatial neighbour graph, built on the normalised copy.
# requires: sq, adata_norm
# params: n_neighs:int
# sample-params: n_neighs = 6
# No tab can register a preview provider for this one: k comes from whichever
# tab called ctx.ensure_spatial_neighbors(), and the Nhood and Ligand-receptor
# tabs each have their own slider. A provider would have to pick one of them
# arbitrarily, so the preview says "a typical k" instead of pretending to know.

#--- block main
# Spatial neighbor graph (k=$n_neighs)
sq.gr.spatial_neighbors_knn(adata_norm, n_neighs=$n_neighs)
